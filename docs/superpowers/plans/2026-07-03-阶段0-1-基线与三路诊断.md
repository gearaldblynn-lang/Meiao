# 阶段0+1:备份基线与三路诊断 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立可回退的备份与基线,然后三路并行诊断(云上日志/本地实测/脏数据审计),产出统一问题清单,作为后续阶段2-5的修复依据与验收基准。

**Architecture:** 阶段0串行(Task 1-4,备份必须最先);阶段1三路子代理并行(Task 5/6/7 互不依赖),Task 8 主会话汇总并复核。云端一切操作只读。

**Tech Stack:** git / rsync / ssh(密钥 `~/.ssh/MEIAO.pem`)/ node --test / 既有云端审计脚本(`scripts/cloud-audit-app-state-health.mjs` 等,纯 SELECT)

**上级设计文档:** `docs/superpowers/specs/2026-07-03-meiao-stability-upgrade-design.md`

**关键事实(执行者必读,勿凭猜测):**
- 项目根:`/Users/feiyanglin/程序开发/电商视觉一键化/版本管理/梅奥MEIAO-当前版本`
- 当前分支 `fix/chunk-error-boundary` 是真正主干(领先 `main` 170 个提交,`main` 已废弃勿用)
- **没有 `npm run test` 脚本**,测试用 `node --test <文件>` 直跑
- 云服务器:`ssh -o IdentitiesOnly=yes -i ~/.ssh/MEIAO.pem root@111.229.66.247`,项目在 `/www/wwwroot/meiao-internal`,PM2 进程名 `meiao-internal`,MySQL 在服务器本机 `127.0.0.1:3307`,库名 `meiao_internal`,凭据在服务器的 `/www/wwwroot/meiao-internal/.env.server`
- **云端禁止任何写操作**:不 UPDATE/DELETE/重启服务/改文件;审计脚本自带只读保护(拒绝 `--apply`)

---

## 阶段0(串行)

### Task 1: 清理工作区,提交悬挂改动

**Files:**
- Modify: 无代码,仅 git 操作
- 涉及: `docs/buyer-show-replication-guide.md`(已修改未提交)、`开发文档/`(未跟踪)

- [ ] **Step 1: 查看悬挂改动内容**

```bash
cd "/Users/feiyanglin/程序开发/电商视觉一键化/版本管理/梅奥MEIAO-当前版本"
git status --short
git diff --stat docs/buyer-show-replication-guide.md
```

预期:`M docs/buyer-show-replication-guide.md` 和 `?? 开发文档/` 两条。

- [ ] **Step 2: 提交文档改动(纯文档,无代码风险)**

```bash
git add docs/buyer-show-replication-guide.md 开发文档/
git commit -m "docs: 收纳买家秀文档改动与重写开发文档(基线前清理工作区)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

- [ ] **Step 3: 确认工作区干净**

```bash
git status --short
```

预期:无输出(dist/ 等已被 .gitignore 覆盖)。若仍有条目,逐条确认来由后再处理,不得 `git clean` 一把梭。

### Task 2: 创建备份版本(业主要求的"可整体替换回去"的保险)

**Files:**
- Create: `/Users/feiyanglin/程序开发/电商视觉一键化/版本管理/梅奥MEIAO-备份版本-260703A/`(整目录)

- [ ] **Step 1: rsync 完整复制(含 .git,排除可再生的 node_modules 与 dist)**

```bash
rsync -a \
  --exclude 'node_modules' \
  --exclude 'dist' \
  "/Users/feiyanglin/程序开发/电商视觉一键化/版本管理/梅奥MEIAO-当前版本/" \
  "/Users/feiyanglin/程序开发/电商视觉一键化/版本管理/梅奥MEIAO-备份版本-260703A/"
```

- [ ] **Step 2: 校验备份完整性**

```bash
cd "/Users/feiyanglin/程序开发/电商视觉一键化/版本管理"
diff <(cd 梅奥MEIAO-当前版本 && git ls-files | sort) \
     <(cd 梅奥MEIAO-备份版本-260703A && git ls-files | sort) && echo BACKUP_OK
```

预期:输出 `BACKUP_OK`(两边 git 跟踪文件清单一致)。

- [ ] **Step 3: 记录恢复方法到备份目录**

写入文件 `/Users/feiyanglin/程序开发/电商视觉一键化/版本管理/梅奥MEIAO-备份版本-260703A/如何恢复.md`:

```markdown
# 恢复方法(2026-07-03 备份)

本备份为稳定性升级动工前的完整快照(基线 tag: baseline-260703)。

整体替换回去:
1. 把 `梅奥MEIAO-当前版本` 改名为 `梅奥MEIAO-坏版本-日期`(留证据,勿直接删)
2. 复制本目录为新的 `梅奥MEIAO-当前版本`
3. 在新目录里 `npm install`(node_modules 未随备份)
4. `npm run local` 验证可启动
```

### Task 3: git 打基线 tag

- [ ] **Step 1: 打 tag 并核对**

```bash
cd "/Users/feiyanglin/程序开发/电商视觉一键化/版本管理/梅奥MEIAO-当前版本"
git tag baseline-260703
git tag -l "baseline*" && git log --oneline -1 baseline-260703
```

预期:列出 `baseline-260703`,指向 Task 1 提交后的 HEAD。

### Task 4: 记录基线健康状态

**Files:**
- Create: `docs/superpowers/diagnostics/2026-07-03-基线健康记录.md`

- [ ] **Step 1: lint**

```bash
cd "/Users/feiyanglin/程序开发/电商视觉一键化/版本管理/梅奥MEIAO-当前版本"
npm run lint 2>&1 | tail -5
```

预期:0 error(基线要求)。若有 error,原样记录进基线文档,**不修**(修复属于后续阶段)。

- [ ] **Step 2: 全量测试(耗时数分钟)**

```bash
node --test 2>&1 | tail -15
```

预期:输出 `# pass N` / `# fail M` 汇总行。记录 N/M 数字。若 `node --test` 无参数不能发现测试,改用:

```bash
find server src scripts -name "*.test.mjs" -not -path "*/node_modules/*" | xargs node --test 2>&1 | tail -15
```

- [ ] **Step 3: build**

```bash
npm run build 2>&1 | tail -5
```

预期:vite build 成功输出 dist 产物。

- [ ] **Step 4: 写基线健康记录并提交**

写入 `docs/superpowers/diagnostics/2026-07-03-基线健康记录.md`(实数填入,不留占位):

```markdown
# 基线健康记录 · baseline-260703

- 日期:2026-07-03
- 基线 commit:<git log --oneline -1 的实际输出>
- lint:<pass / N errors>
- node --test:pass <N> / fail <M>(命令:<实际用的命令>)
- build:<成功/失败>
- 备份位置:版本管理/梅奥MEIAO-备份版本-260703A

后续每阶段收尾必须:lint 不新增 error、test pass 数 ≥ 本基线、build 成功。
```

```bash
git add docs/superpowers/diagnostics/2026-07-03-基线健康记录.md
git commit -m "docs: 基线健康记录(baseline-260703)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## 阶段1(Task 5/6/7 三路子代理并行,Task 8 主会话汇总)

> 并行纪律:三个子代理各自只写自己的报告文件,不改任何代码、不共享文件,无冲突。
> 子代理报告中的统计数字,Task 8 由主会话抽样复核后才可写入问题清单(经验库铁律)。

### Task 5(子代理A): 云上日志诊断(只读)

**Files:**
- Create: `docs/superpowers/diagnostics/2026-07-03-云上日志诊断.md`

- [ ] **Step 1: 确认 SSH 连通与只读边界**

```bash
ssh -o IdentitiesOnly=yes -i ~/.ssh/MEIAO.pem root@111.229.66.247 "pm2 ls && ls /www/wwwroot/meiao-internal/.env.server"
```

预期:看到 `meiao-internal` 进程 online。**此后所有远端命令仅限 SELECT / cat / tail / pm2 logs,禁止任何写操作。**

- [ ] **Step 2: 摸清 internal_logs 表结构(先看结构再查,不猜列名)**

```bash
ssh -o IdentitiesOnly=yes -i ~/.ssh/MEIAO.pem root@111.229.66.247 '
  set -a; source <(grep -E "^MEIAO_DB_" /www/wwwroot/meiao-internal/.env.server); set +a
  mysql -h"$MEIAO_DB_HOST" -P"$MEIAO_DB_PORT" -u"$MEIAO_DB_USER" -p"$MEIAO_DB_PASSWORD" "$MEIAO_DB_NAME" \
    -e "SHOW COLUMNS FROM internal_logs; SELECT COUNT(*) AS total FROM internal_logs;"
'
```

预期:列名清单 + 总行数。**根据实际列名**调整 Step 3 的查询字段(比如级别列可能叫 level/severity,时间列可能叫 created_at/ts)。

- [ ] **Step 3: 统计近7天报错分布(字段名按 Step 2 实际结果替换)**

```bash
ssh -o IdentitiesOnly=yes -i ~/.ssh/MEIAO.pem root@111.229.66.247 '
  set -a; source <(grep -E "^MEIAO_DB_" /www/wwwroot/meiao-internal/.env.server); set +a
  mysql -h"$MEIAO_DB_HOST" -P"$MEIAO_DB_PORT" -u"$MEIAO_DB_USER" -p"$MEIAO_DB_PASSWORD" "$MEIAO_DB_NAME" -e "
    SELECT <级别列>, COUNT(*) AS n FROM internal_logs
      WHERE <时间列> >= NOW() - INTERVAL 7 DAY GROUP BY <级别列> ORDER BY n DESC;
    SELECT <消息列>, COUNT(*) AS n FROM internal_logs
      WHERE <时间列> >= NOW() - INTERVAL 7 DAY AND <级别列> IN (<错误级别值>)
      GROUP BY <消息列> ORDER BY n DESC LIMIT 30;"
'
```

预期:错误级别分布 + 出现频次最高的 30 类报错原文。

- [ ] **Step 4: 抓 PM2 近期错误日志样本**

```bash
ssh -o IdentitiesOnly=yes -i ~/.ssh/MEIAO.pem root@111.229.66.247 \
  "pm2 logs meiao-internal --err --lines 200 --nostream"
```

- [ ] **Step 5: 写诊断报告**

写入 `docs/superpowers/diagnostics/2026-07-03-云上日志诊断.md`,必须包含:
1. 报错类型排行榜(报错原文 + 7天频次 + 初步归类:调度类/数据类/上游类)
2. 每类报错附 1 条完整样本(含时间戳、上下文字段)
3. 明确写"本报告全部来自只读查询,查询命令原文附后"

### Task 6(子代理B): 本地实测巡检

**Files:**
- Create: `docs/superpowers/diagnostics/2026-07-03-本地实测巡检.md`

- [ ] **Step 1: 环境体检并启动本地服务**

```bash
cd "/Users/feiyanglin/程序开发/电商视觉一键化/版本管理/梅奥MEIAO-当前版本"
npm run doctor 2>&1 | tail -20
npm run local > /tmp/meiao-local-dev.log 2>&1 &
sleep 15 && tail -30 /tmp/meiao-local-dev.log
```

预期:doctor 无致命项;local-dev 日志出现服务就绪标志(端口监听)。从日志确认前后端实际端口。

- [ ] **Step 2: 逐板块发真实请求**

巡检板块与顺序:一键主详 → 买家秀 → 短视频 → 翻译 → 智能体对话 → 智能工厂(配置/知识库/工具/预览/发布)。

方法:
1. 先从 `src/services/internalApi.ts` 查该板块实际调用的 API 路径(不猜 URL)
2. 用 curl 对该 API 发起真实请求(带登录态:先调登录接口拿会话)
3. 记录:请求→响应→任务状态流转(轮询状态接口直到终态或超时5分钟)

每板块记录一条结论:走通 / 卡住(卡在哪个状态) / 报错(报错原文)。

- [ ] **Step 3: 对"卡住/不生效"场景上运行态探针**

对每个卡住场景,在怀疑的 server 端函数入口加写文件探针(改完记得看,验证完撤销):

```js
// 探针模板:插到怀疑函数第一行,验证完删除
import { appendFileSync } from 'node:fs';
appendFileSync('/tmp/meiao-probe.log', `${new Date().toISOString()} 进入 <函数名> ${JSON.stringify(<关键参数>)}\n`);
```

重启本地服务→复发请求→看 `/tmp/meiao-probe.log`:**日志不出现本身就是铁证**(请求根本没走这里),立即改查实际路径;日志出现则记录参数快照。探针结论写进报告后,`git checkout -- <文件>` 撤销所有探针改动。

- [ ] **Step 4: 写巡检报告**

写入 `docs/superpowers/diagnostics/2026-07-03-本地实测巡检.md`,每条问题必须含:板块、复现步骤(可照做)、预期 vs 实际、探针钉死的实际代码路径(文件:行号)、初步归类(调度类/数据类/交互类)。

最后确认探针已全部撤销:`git status --short` 只应剩本报告文件。

### Task 7(子代理C): 脏数据审计(本地+云端,全部只读)

**Files:**
- Create: `docs/superpowers/diagnostics/2026-07-03-脏数据审计.md`

- [ ] **Step 1: 云端 app_states 健康审计(复用既有只读脚本,在服务器上跑)**

```bash
ssh -o IdentitiesOnly=yes -i ~/.ssh/MEIAO.pem root@111.229.66.247 \
  "cd /www/wwwroot/meiao-internal && node scripts/cloud-audit-app-state-health.mjs --json --limit=100" \
  > /tmp/cloud-app-state-health.json
python3 -c "import json;d=json.load(open('/tmp/cloud-app-state-health.json'));print(json.dumps(d,ensure_ascii=False,indent=1)[:3000])"
```

预期:JSON 汇总(异常任务身份/展示状态统计)。若脚本报参数错误,先看脚本头部注释的实际用法再调。

- [ ] **Step 2: 云端 state 体积统计(同为只读)**

```bash
ssh -o IdentitiesOnly=yes -i ~/.ssh/MEIAO.pem root@111.229.66.247 \
  "cd /www/wwwroot/meiao-internal && node scripts/cloud-stat-state-sizes.mjs 2>&1 | tail -30"
```

预期:各账号 state_json 体积分布(对照经验库"单行 JSON 撑爆 max_allowed_packet"那条,看有没有逼近 4MiB 守卫的)。

- [ ] **Step 3: 干跑修复脚本看它"想修什么"(dry-run,不落地)**

```bash
ssh -o IdentitiesOnly=yes -i ~/.ssh/MEIAO.pem root@111.229.66.247 \
  "cd /www/wwwroot/meiao-internal && node scripts/cloud-dry-run-app-state-repair.mjs 2>&1 | tail -40"
```

预期:列出可修复项清单(这就是脏数据存量的种类与数量)。**绝不带 --apply。**

- [ ] **Step 4: 本地库同样审计**

本地起服后对本地数据跑同样的健康分析(本地脚本同目录,读本地 env),记录本地脏数据形态是否与云端一致。

- [ ] **Step 5: 写审计报告**

写入 `docs/superpowers/diagnostics/2026-07-03-脏数据审计.md`,必须含:
1. 脏数据分类清单:每类给「形态描述 + 数量(云端/本地) + 疑似产生源头(哪个写入点)」
2. 与 `docs/cloud-update-data-cleanup.md` 已知垃圾卡形态的对照(哪些是老熟人、哪些是新形态)
3. 清理风险标注:哪些可安全清、哪些要业主确认

### Task 8(主会话): 汇总问题清单 + 数字复核

**Files:**
- Create: `docs/superpowers/diagnostics/2026-07-03-问题清单.md`

- [ ] **Step 1: 复核三份子代理报告的关键数字**

对每份报告抽 2-3 个关键统计,主会话亲自重跑对应命令核对(经验库铁律:subagent 数字必须自己验)。不一致的以复核结果为准并在清单标注。

- [ ] **Step 2: 合并成统一问题清单**

写入 `docs/superpowers/diagnostics/2026-07-03-问题清单.md`,格式:

```markdown
# 问题清单(阶段2-5 修复与验收基准)

## 调度类
| # | 问题 | 复现步骤 | 线上7天频次 | 实际代码路径 | 修复阶段 |
## 数据类
| # | 脏数据形态 | 数量(云/本地) | 产生源头 | 清理风险 | 修复阶段 |
## 交互类
| # | 问题 | 场景 | 影响 | 修复阶段 |
```

每条分派到修复阶段(阶段2=模型调度类 / 阶段3=任务状态+数据类 / 阶段4=交互类 / 阶段5=智能工厂类)。

- [ ] **Step 3: 提交全部诊断产出**

```bash
cd "/Users/feiyanglin/程序开发/电商视觉一键化/版本管理/梅奥MEIAO-当前版本"
git add docs/superpowers/diagnostics/
git commit -m "docs: 阶段1三路诊断报告与统一问题清单

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

- [ ] **Step 4: 呈报业主**

把问题清单讲给业主听(结论→原因→下一步,人话),由业主确认优先级后,再编写「计划二:阶段2 模型调度中心」。

---

## 验收标准(本计划)

1. 备份目录存在且校验 `BACKUP_OK`,含恢复说明
2. tag `baseline-260703` 存在,基线健康记录已提交(lint/test/build 实数)
3. 三份诊断报告 + 统一问题清单已提交,关键数字经主会话复核
4. 云端零写操作(全程只有 SELECT/cat/tail/pm2 logs/dry-run)
5. 本地代码零改动残留(探针全部撤销,git status 干净)
