# 阶段2:任务状态中枢·第一批(S1/D1/S5/E/文档) 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 修掉诊断问题清单中最高优先级的调度与数据源头问题:worker 静默死亡不可见(S1)、无身份占位永久落库(D1增量)、本地 admin 登录 401(S5)、将离重复任务身份(E),并同步文档(F)。

**Architecture:** 全部在既有单一判据上扩展,不新建平行实现。S1 在 `/api/health` 加 poller 维度(Temporal gRPC describeTaskQueue),`local-dev` 复用端口前先体检;D1 在服务端存储合并层复用 `appStateHealth` 的"无身份活跃占位"判据做写入时标失败;S5 让本地 JSON 模式启动时幂等补建 env admin;E 先探针钉根因再修。

**Tech Stack:** Node --test / @temporalio/client 1.16.2 (`connection.workflowService.describeTaskQueue`) / 既有 `server/appStateHealth.mjs`、`server/appStateRepairPlan.mjs`

**分支:** `feat/stability-phase2`(从 fix/chunk-error-boundary 切出)
**基线约束:** lint 0 error 不新增;测试 pass ≥ 1465;build 成功
**关键事实:**
- `/api/health`:`server/index.mjs:14267`,现只返回 `{ok,mode,taskEngine}`
- local-dev 复用逻辑:`scripts/local-dev.mjs:55-75`(见 node 占用即复用,不体检)
- 本地 admin 只在 `ensureLocalStore`(`server/index.mjs:1598`)store 文件不存在时创建;store 已存在则 env admin 缺失 → 登录 401
- MySQL 模式引导在 `server/index.mjs:2837`(users 表空时),**保持不动**(生产语义不同)
- D1 判据已存在:`server/appStateHealth.mjs` `active_result_without_identity`;修复动作已存在:`appStateRepairPlan.mjs` `mark_active_result_without_identity_failed`
- 改 `appStateMerge.mjs` 属"合并/现算"雷区:动手前必写最小输入对比探针(全局铁律)

---

### Task F: 文档同步(16MiB 闸值)— 最小任务,先做

**Files:** Modify `.env.server.example`、`docs/tencent-cloud-deploy.md`

- [ ] Step 1: `.env.server.example` 中 `APP_STATE_MAX_BYTES` 注释与建议值更新为 16777216,注明"2026-07-04 云上已实施(妙木山超8MiB闸丢项目触发),MySQL max_allowed_packet=128MiB,仍有8倍余量"
- [ ] Step 2: `docs/tencent-cloud-deploy.md` env 清单加同款说明
- [ ] Step 3: commit `docs: 同步 APP_STATE_MAX_BYTES=16MiB 云上实施值`

### Task A: S1 — `/api/health` 加 worker poller 维度 + local-dev 复用前体检

**Files:**
- Create: `server/workerHealth.mjs`、`server/workerHealth.test.mjs`
- Modify: `server/index.mjs:14267-14273`(health handler)、`scripts/local-dev.mjs:55-75`、`scripts/local-dev-utils.mjs`(+its test)

- [ ] Step 1: 写失败测试 `server/workerHealth.test.mjs`:
  - `checkTemporalWorkerHealth({describeTaskQueue})`(依赖注入 fake):pollers 非空 → `{healthy:true,pollerCount:N}`;pollers 空 → `{healthy:false,pollerCount:0}`;gRPC 抛错 → `{healthy:false,error:'...'}`(不抛,降级返回)
  - 结果带 `checkedAt` 毫秒戳;5s 超时防挂
- [ ] Step 2: `node --test server/workerHealth.test.mjs` 确认 FAIL
- [ ] Step 3: 实现 `server/workerHealth.mjs`:懒加载 `@temporalio/client` Connection(复用 temporalTaskAdapter 的 config 解析:address/namespace/taskQueue env),调 `connection.workflowService.describeTaskQueue({namespace, taskQueue:{name}, taskQueueType:1})`(1=WORKFLOW;再查 taskQueueType:2 ACTIVITY,两者任一有 poller 计 healthy;分别返回计数);内存缓存 10s(env `MEIAO_WORKER_HEALTH_CACHE_MS`)避免 health 高频打 gRPC
- [ ] Step 4: 测试过后接入 `/api/health`:`taskEngine==='temporal'` 时附加 `worker:{healthy,workflowPollers,activityPollers,checkedAt}`;非 temporal 模式返回 `worker:{healthy:true,engine:'mysql'}`(不误报)。health 本身永远 200(worker 状态是数据不是可用性)
- [ ] Step 5: `scripts/local-dev.mjs` 复用 3100 前:GET `/api/health`,若 `worker.healthy===false` → 打印「3100 后端 HTTP 存活但 temporal worker poller 已死(常驻 launchd 实例僵死),任务将永远排队。请执行: kill <pid> 让 launchd 重生,或 launchctl kickstart -k …」并 `process.exit(1)`,**不静默复用**。判定函数抽到 `local-dev-utils.mjs` 配单测
- [ ] Step 6: 全量相关测试 + lint;commit `fix(S1): health 暴露 temporal poller 状态,local-dev 拒绝复用僵死后端`

### Task B: D1 增量 — 存储合并层把"无身份活跃占位"超窗标失败

**Files:**
- Modify: `server/appStateMerge.mjs`(mergeAppStateForStorage 出口处)、`server/appStateMerge.test.mjs`
- 判据复用: `server/appStateHealth.mjs` 的无身份活跃占位识别(如未导出则导出),修复动作语义对齐 `appStateRepairPlan.mjs` 的 `mark_active_result_without_identity_failed`

- [ ] Step 1: **先写输入对比探针**(铁律):~10行脚本把「含无身份 generating 占位(createdAt 早于窗口)」的 state 分别喂 `buildAppStateRepairPlan` 和现状 `mergeAppStateForStorage`,确认前者会修、后者原样放行——钉死"写入层无守卫"
- [ ] Step 2: 失败测试:无身份活跃占位、age > `MEIAO_IDENTITYLESS_ACTIVE_TTL_MS`(默认 21600000 = 6h,保守:远大于任何正常提交耗时)→ 存储时标 `status:'error'` + `errorCode:'identityless_placeholder_expired'` + 结构化失败字段(遵循#2:结构化优先);age 窗口内 → 原样保留(不误杀刚提交的);有 backendJobId/providerTaskId 的活跃结果 → 永不动
- [ ] Step 3: 实现:抽 appStateHealth 的判据为导出函数,mergeAppStateForStorage 出口对 shellProjects/各模块 memory 桶统一应用;env + 保守默认
- [ ] Step 4: 回归:`appStateMerge.test.mjs` + `stateReconciliationConsistency.test.mjs` 全过(前后端一致性契约不破坏)
- [ ] Step 5: commit `fix(D1): 存储层过期无身份占位标失败,阻断脏数据增量`

### Task C: S5 — 本地 JSON 模式幂等补建 env admin

**Files:** Modify `server/index.mjs`(ensureLocalStore 附近)、新测试(局部函数级)

- [ ] Step 1: 失败测试:store 已存在但无 env admin 用户名 → 启动后该用户存在(role admin/active);已存在同名用户 → 不重复建、不改密码(幂等);MySQL 模式路径零改动
- [ ] Step 2: 实现 `ensureLocalAdminUser(store)`:`store.users` 无 `username===process.env.MEIAO_ADMIN_USERNAME||'admin'` 时用 `createUser` 补建并 `writeLocalStore`;在本地模式启动路径调用
- [ ] Step 3: 手工验证:`npm run local` 后 curl 登录 env admin → 200
- [ ] Step 4: commit `fix(S5): 本地模式幂等补建 env admin,修登录 401`

### Task E: 将离 duplicate_task_identity ×2 — 先钉根因再修

**Files:** 待探针结果定;可能涉及 `src/utils/taskResultReconcile.mjs` 或一次性数据修复

- [ ] Step 1: 云上只读拉将离 state 中 2 条重复身份的完整 JSON(项目/结果/身份字段),本地探针重放 `taskResultReconcile` 看是否该去重而未去重
- [ ] Step 2: 分叉:(a) 代码 bug → TDD 修 reconcile;(b) 纯历史数据 → 扩展 repair plan 加 `dedupe_task_identity` 动作,dry-run→apply(云上写操作我亲自执行)
- [ ] Step 3: 复查健康审计 `duplicate_task_identity` 归零;commit

### 收尾

- [ ] 全量 `node --test`(pass ≥ 1465)+ lint + build
- [ ] 更新问题清单勾掉 S1/D1/S5/E 并注修复 commit
- [ ] 沉淀根因库(有资格的坑)
