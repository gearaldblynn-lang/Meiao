# 阶段3:模型调度中心·第一批(S2/S3/调度入口) 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development / executing-plans。checkbox 跟踪。

**Goal:** 落地模型调度中心的后端核心:①S2 上游传输韧性(云上最大故障源 fetch failed×191/7天)②S3 余额预警 ③模型能力标签+单一调度入口,收敛散落的 env 模型选择。

**Architecture:** 不新建平行体系——扩展既有 `modelProviderRegistry.mjs`(档案),新建 `server/modelDispatch.mjs`(唯一调度入口),S2/S3 在 `providerGateway.mjs`/`jobRuntime.mjs` 既有错误分类与重试机制上收敛强化。前端设置界面与逐模块迁移属下一批。

**分支:** 继续 `feat/stability-phase2`
**基线约束:** lint 0 error;测试 pass ≥ 1484(当前);build 成功;7 个既有失败之外零新增
**关键事实(已核实):**
- 模型档案:`server/modelProviderRegistry.mjs`(254行),models 已有 `{id, mode(chat/embedding/rerank/image/video), features[]}`,normalize 链路齐全
- 散落的模型选择:env `KIE_CHAT_MODEL`/`MEIAO_DEFAULT_CHAT_MODEL`/`MEIAO_PLANNING_ANALYSIS_MODEL`/`MEIAO_AGENT_ANALYSIS_MODEL` 散在 `server/index.mjs`(resolveImageModel:1304、resolveChatFallbackModels:1347)、`jobRuntime.mjs`、`providerGateway.mjs`
- S2 现状:`providerGateway.mjs` 已有 `provider_network_error`/`provider_timeout` 分类(:215,:934,:1085)、模型 fallback(`runKieChatFallbackModels`,asset_upload 阶段不 fallback #23);`jobRuntime.mjs` 有 `RETRYABLE_ERROR_CODES` + `getNextJobFailureState`(asset_upload 瞬时重试1次 #27)
- S3 现状:`provider_credit_insufficient` 分类已存在(providerGateway.mjs:152-155),但只进日志,用户看到裸失败
- 雷区:根因库 #23(fallback 不得用于传输阶段)、#2(结构化优先)、#33(并发判据单一);改 providerGateway 必跑 `node --test server/providerGateway.test.mjs`(4482行测试)

---

### Task G(S2): 传输阶段瞬时错误统一重试 + 失败人话化

**Files:**
- Modify: `server/providerGateway.mjs`(kie HTTP 请求层)、`server/jobRuntime.mjs`(重试预算)
- Create: `server/providerErrorHumanize.mjs` + test
- Modify: `.env.server.example`(新旋钮)

- [ ] Step 1: 读 providerGateway 的 kie fetch 封装(:215 附近)与 jobRuntime `RETRYABLE_ERROR_CODES`/`getNextJobFailureState`,写探针脚本确认现状:provider_submit/provider_wait 阶段遇 `fetch failed` 当前重试几次(报告贴结论)
- [ ] Step 2: TDD:HTTP 请求层对 `fetch failed`/ECONNRESET/502/503/504 加**请求级**有界重试(env `MEIAO_KIE_HTTP_TRANSIENT_RETRIES` 默认 2,指数退避 1s/3s,env `MEIAO_KIE_HTTP_RETRY_BASE_MS`);仅幂等读操作与提交前阶段直接重试,`createTask` 类提交操作只在**确认未收到响应体**时重试(防重复扣费——宁可失败不可重复提交);与既有任务级重试(#27)叠加但预算独立
- [ ] Step 3: TDD `providerErrorHumanize.mjs`:单一映射表 errorCode→人话(`provider_network_error`→"服务器到生成服务的网络暂时不稳,系统已自动重试,请稍后再试";`provider_credit_insufficient`→"生成服务余额不足,请联系管理员充值";`provider_timeout`/`provider_internal_error`/`cpu overloaded`/`Concurrency limit` 等全覆盖诊断报告 TOP 报错);job 失败落库时 `error`=人话 + `errorDetail`=技术原文(结构化,两字段并存,不丢排障信息)
- [ ] Step 4: 回归 providerGateway.test.mjs + jobRuntime.test.mjs 全过;commit `fix(S2): 传输瞬时错误请求级重试+失败原因人话化`

### Task H(S3): KIE 余额预警

**Files:**
- Modify: `server/providerGateway.mjs`(credit 错误处)、`server/index.mjs`(/api/health 或管理员通知面)
- Create: `server/creditAlert.mjs` + test

- [ ] Step 1: 查 KIE 有无余额查询 API(读 providerGateway 现有 kie 端点清单/docs,**不臆测**;没有就走错误驱动)
- [ ] Step 2: TDD `creditAlert.mjs`:遇 `provider_credit_insufficient` 时记录 `{provider,lastAt,count}`(内存+internal_logs 显著级别);`/api/health` 附 `creditAlert` 快照(管理员可见);同一 provider 预警节流(env 默认 1h 一次显著日志,防刷屏)
- [ ] Step 3: 有余额 API 则加低余额阈值主动探测(env 开关,默认关);无则跳过并在报告注明
- [ ] Step 4: commit `feat(S3): KIE 余额不足预警(health 快照+节流日志)`

### Task I: 模型能力标签 + 单一调度入口

**Files:**
- Modify: `server/modelProviderRegistry.mjs`(能力标签常量+校验)
- Create: `server/modelDispatch.mjs` + test
- Modify: `server/index.mjs` 四个 env 模型选择点改走 dispatch(行为兼容)

- [ ] Step 1: 能力标签 schema:在 features 词汇表基础上定义标准能力集 `streaming`/`cache-hit`/`web-search`/`tool-call`/`vision`/`direct-result`(任务型直出)/`reasoning`,`normalizeModelEntry` 校验归一(未知标签保留不丢)
- [ ] Step 2: TDD `modelDispatch.mjs` 单一入口:`resolveModelForNeed({need, capabilities=[], preferredModel='', registry, env})` → `{model, provider, baseUrl, endpointPath, fallbackModels}`;解析顺序:显式 preferredModel(用户配置永远最高)→ env 兼容层(KIE_CHAT_MODEL 等四个,保持现值语义)→ 注册表按 need+capabilities 匹配 → 保守默认。**纯函数,依赖注入 registry/env,行为可测**
- [ ] Step 3: 迁移四个 env 散点(index.mjs 的 analysis/chat 默认解析)到 dispatch 调用,输入对比探针验证迁移前后同输入同输出(铁律),行为零变化
- [ ] Step 4: 回归全量相关测试;commit `feat: 模型能力标签+单一调度入口 modelDispatch(env 兼容迁移)`

### 收尾
- [ ] 全量测试(pass≥1484 且失败与基线同名)+ lint + build
- [ ] 问题清单勾 S2/S3;沉淀;向业主汇报(含"是否发布"决策点)
