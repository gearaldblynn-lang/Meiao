# 图片升级对比查看器缩放实施计划

1. 在 `retouchComparison.ts` 增加缩放、平移边界和指针中心缩放的纯函数，并先补失败测试。
2. 在 `RetouchComparisonViewer.tsx` 增加缩放/平移状态、同步图片变换、滚轮和指针手势。
3. 增加紧凑的缩放工具栏、百分比、复位入口和操作提示，并补组件契约测试。
4. 运行对比查看器相关测试、TypeScript/Vite 构建和本地 doctor。
5. 用现有产品还原素材在浏览器验证同步缩放、平移、遮罩对比、复位、切图和刷新后的可用性。
