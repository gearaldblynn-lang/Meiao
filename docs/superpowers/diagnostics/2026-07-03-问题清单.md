# 问题清单(阶段2-5 修复与验收基准)

日期:2026-07-03 · 来源:三路诊断(云上日志/本地实测/脏数据审计),关键数字经主会话复核
基线:tag `baseline-260703`,测试 1465/1472,lint 0 error,build 成功

## 已完成的紧急止血(记录在案)

| 动作 | 结果 |
|---|---|
| 云上 `APP_STATE_MAX_BYTES` 8→16MiB(`.env.server` 已改+备份为 `.env.server.bak-260703-guard8m`,进程环境已验证 16777216,pm2 save 完成) | 妙木山(8.76MiB,已超8MiB闸正在丢老项目)、晓龙(6.96MiB)脱离风险。MySQL `max_allowed_packet`=128MiB,16MiB 闸仍有 8 倍余量 |

⚠️ 遗留同步义务:本地仓库 `.env.server.example` 与 `docs/tencent-cloud-deploy.md` 中该旋钮的建议值需同步(阶段3 顺手做)。

## 调度类

| # | 问题 | 证据/频次 | 实际代码路径 | 修复阶段 |
|---|---|---|---|---|
| S1 | ✅**已修(2026-07-06,commit b377d91)** Temporal worker poller 静默死亡不可见 → `/api/health` 现带 `worker:{healthy,workflowPollers,activityPollers}`(server/workerHealth.mjs,gRPC describeTaskQueue,10s缓存);`local-dev` 复用 3100 前体检,poller 死则拒绝并给 kill/kickstart 指引 | 本地实测复现+复核确认 | `scripts/local-dev.mjs:55-75`(复用不体检)、`/api/health` handler(无 poller 维度) | ~~阶段3~~ 已修 |
| S2 | ✅**已修(2026-07-06,commits 3bc5435/c25e400)** 请求级瞬时重试(连接层错误全类可重试,5xx 只重只读 GET,提交类 POST 收到响应零重试防重复扣费)+ 失败人话化(errorMessage=人话/errorDetail=技术原文,providerErrorHumanize 单一映射)。原描述:服务器→KIE 慢性网络故障是云上最大单一故障源:`fetch failed` ×191/7天(跨 6/26~7/3、5+模块),另有结果资源 404 ×32、中转站过载(503 cpu overloaded/Concurrency limit)≈30。程序把传输失败裸暴露成任务失败 | 云上 internal_logs(主会话复核:err 总 408,fetch failed 191,provider_submit_failed 184) | `providerGateway.mjs` 提交/等待链路 | 阶段2(调度中心内建:传输阶段重试与模型阶段分离、上游健康探测、失败人话化) |
| S3 | ✅**已修(2026-07-06,commit b17b173)** creditAlert 模块:失败链路三接入点(MySQL/Temporal/本地)遇 credit_insufficient 记内存状态+节流显著日志(1h),/api/health 附 creditAlert 快照;KIE 无余额查询端点(查证过),故为错误驱动预警 | 余额不足×6+积分不足×5 | providerGateway 错误分类 | 已修 |
| S4 | 部署后旧页面引用旧 hash chunk(`Failed to fetch dynamically imported module`×2)——#5 修复覆盖不到"入口 HTML 本身是旧的"场景 | 云上日志 | 发布策略(dist 原子切换/旧 chunk 保留期) | 阶段4 |
| S5 | ✅**已修(2026-07-06,commit 44556f3)** 本地 env admin 登录 401 → `server/localAdminBootstrap.mjs` 启动幂等补建(已存在同名零改动)。视频权限守卫两份站点(`index.mjs:10811`/`14060`)共用同一函数 `canUseVideoGenerationFeature`,非漂移,不需收敛 | 本地实测 | `server/index.mjs:11069` 本地登录 handler | ~~阶段3~~ 已修 |

## 数据类

| # | 脏数据形态 | 数量(云/本地) | 产生源头 | 清理风险 | 修复阶段 |
|---|---|---|---|---|---|
| D1 | 无身份活跃占位结果(拿到 backendJobId 前先落 generating 占位,中断后永远"生成中") **存量已清(2026-07-04,140动作/21账号);增量守卫已修(2026-07-06,commit b41641c):存储合并层对超6h(env MEIAO_IDENTITYLESS_ACTIVE_TTL_MS)无身份占位标结构化失败(errorCode=identityless_placeholder_expired),判据单一复用 appStateHealth。⚠️守卫未上云前云上仍会新增(7/6 复查已+19条),发布后自愈** | 57 / 0 | `ShellMigratedApp.tsx:3699` 等写入点 | 可安全标失败 | ✅代码已修,待发布 |
| D2 | ✅存量已清(2026-07-04 repair apply 39条 normalize) 完成态计数漂移 | 42 / 7 | 根因库#1 修复前历史存量 | 可安全归一(dry-run 已验证:妙木山10条 normalize 动作) | 阶段3 |
| D3 | 完成但无输出(四个子形态:三七 job-* 翻译卡21、多桑翻译卡5、分镜"待出图"9、一键策划卡6) | 39 / 7 | 翻译卡:prompt 原文当项目名 `shellDataAdapter.ts:1796`;分镜9条疑似**判据误报**非脏数据 | 分镜卡先修判据再谈清理;三七卡须业主确认 | 阶段3 |
| D4 | ✅存量已清(2026-07-04 apply,113张) 无效项目卡 | 113张/15用户 | 多源 | dry-run 清单已有(样例:proj-1782293218193 等) | 阶段3 |
| D5 | ✅存量已清(2026-07-04 apply:终态114+策划273,业主已授权) 孤儿job | 273+114 / 566+207 | job 与项目卡失联 | 孤儿终态可安全清;273条策划 job 须业主确认 | 阶段3 |
| D6 | 素材没有可用于模型读取的公网地址 ×8 + JSON 控制字符解析错误 ≈40(历史) | 云上 | 写入侧未校验 | 增量堵源头 | 阶段3 |
| D7 | ✅**已修(2026-07-06,commit 77c0a72)** 根因:检索默认相似度阈值漂移为 1,过滤掉全部向量结果;统一为单一常量 `DEFAULT_SIMILARITY_THRESHOLD = 0.3`(knowledgeRetrieval.mjs),ingestion/rag 两处消费点改为引用 | 本地 | ~~待钉死~~ 已钉死 | — | ~~阶段5~~ 已修 |

## 交互类

| # | 问题 | 场景 | 修复阶段 |
|---|---|---|---|
| U1 | ✅**已修(2026-07-06,commit 250bdc8)** 结构化错误(S2 的 errorMessage 人话/errorDetail 技术原文)+ 前端失败卡展示人话原因,附「查看技术详情」折叠入口(errorDetail 只读透传,空则不显示) | 全板块 | ~~阶段2+4~~ 已修 |
| U2 | ✅**已修(2026-07-06,commits d325f36/2304d07)** agent DELETE 路由(双模式 MySQL+本地 JSON 都有)+ PATCH enabled 进白名单;会话级联删除;拒删最后一个 agent;前端删除/停用按钮带二次确认 | 智能工厂 | ~~阶段5~~ 已修 |
| U3 | "卡处理中"的观感问题:后台其实在跑/已成功,前端占位没被覆盖(与 S1/D1 强相关,先修调度再看残余) | 全板块 | 阶段3后复测,残余归阶段4 |

## 板块巡检结论(基准面)

一键主详 ✅(~100s 出真图)/ 买家秀 ✅(51s succeeded)/ 短视频 ✅(权限守卫按设计放行 admin,B 的 403 猜想证伪)/ 翻译 ✅(排除 S1 后正常)/ 智能体对话 ✅(真实回复+知识库检索 chunk×5)/ 智能工厂:配置✅ 知识库✅ 发布✅,工具执行与预览卡**本地配置缺失**(环境变量/API Key 未配),非代码 bug。

## 已证伪/排除(防止后人再查)

- 前后端任务终态词均为 `succeeded`,不存在"终态词不一致导致卡处理中"
- PM2 ↺547 是人为部署重启累计,近7天 0 次异常退出
- 短视频 `videoGeneration:false` 不拦 admin 是**设计如此**(`index.mjs:1083` 对 admin 直接放行)
- 老熟人"空视频诊断卡"0 新增;未发现账号串号

## 悬而未决(等业主)

1. 备份目录内 148M 冗余嵌套 `梅奥MEIAO-备份版本-260703A-tmp/` 删否
2. D3 三七的 21 张翻译卡、D5 的 273 条孤儿策划 job:清理须逐项确认
3. 智能工厂巡检残留:改名「巡检残留,可删」的 agent 一个(受 U2 所限删不掉)

## 修复进度(2026-07-06 · feat/stability-phase2)

- Task E ✅:将离 duplicate_task_identity ×2 已清零——根因是 2026-06-10 历史合成卡(job-* 翻译卡,D3 形态)与批量项目重复认领同一结果;带守卫原子 UPDATE 清除(备份 /root/backup-jiangli-appstate-260706.sql.gz)
- 回归:1491 测试 1484 过 / 7 失败(与基线 7 条完全同名,零新增失败);lint 0 error;build 成功
- 分支待发布:S1/D1/S5 三修复在 feat/stability-phase2,发布云上后 D1 增量自愈

## 修复进度追加(2026-07-06 · 阶段3第一批)
- Task G(S2)✅ / Task H(S3)✅ / Task I(模型能力标签+modelDispatch 单一调度入口,env 散点迁移第一批,审查员独立2150组探针 ALL-EQUAL)✅
- 全量回归:1545 测试 1538 过,7 失败与基线同名零新增;lint 0 error;build 成功
- 模型调度中心后续批次:~~jobRuntime env 读取点迁移~~(✅第二批已完成)、前端 GlobalApiSettings 统一入口、逐模块能力声明迁移

## 修复进度追加(2026-07-06 · 三路并行批次 W1/W2/W3)
- W1 ✅:jobRuntime 模型 env 读取点迁移到 modelDispatch(1024组输入对比探针 ALL-EQUAL)+ userFeaturePermissionsSource 过时断言修复(commits 526ac67/1c6c693,合并 c4f27fc)
- W2 ✅:U2 agent 删除/停用 + D7 阈值统一(commits 77c0a72/d325f36/2304d07,合并 52862d6);巡检残留 agent 现在可删
- W3 ✅:U1 失败卡人话+技术详情入口(250bdc8);6个前端域基线失败测试全部诊断修复——5处断言过时(逐个 git log -S 考古确认),1处真回归:shellWorkflow.ts 被禁的 Date.now requestId(破坏 3680429 去重保护,a1251d8 重新引入)已移除(f6a24d1);DELETE 路由新增2个 requireUser 守卫,计数断言 21→22(b779c86)
- 全量回归:1553 测试全过(**基线的 7 条失败全部清零,零凑绿——每条有考古证据**);lint 0 error;build 成功;worktree/批次分支已清理
- 当前未修:S4(部署 chunk hash,阶段4)、D3 部分(三七21卡待业主确认+分镜判据)、D6(素材公网地址校验)、U3(上云后复测残余)

## 修复进度追加(2026-07-07 · 上云与收尾)
- 分镜判据误报 ✅(commit b6b5daa):`isStoryboardPlanningProject` 单一判据——storyboard 桶+有 script/shots/boards 即"策划完成"有效态,审计/修复器都豁免;真空壳仍上报。本地探针:旧判据误报 14 条→0
- D3 剩余 ✅(commit 8f68774):探针发现 18 张"完成无输出"卡(大善翻译 7、朱演 13、南烛 1;原审计口径的三七/多桑卡已在前几轮清理中消化)的 backendJobId 全指向 succeeded+imageUrl 的 job——是回填断链不是真丢。新脚本 `cloud-backfill-completed-without-output.mjs` 回填而非删除,云上 apply 3 用户 32 动作,幂等复跑 0 动作
- **feat/stability-phase2 已发布腾讯云 ✅**(2026-07-07,过 MEIAO_CODE_REVIEW_CONFIRMED+security:audit 门禁):`/api/health` 现返回 `worker:{healthy:true,workflowPollers:2,activityPollers:2}`(S1 生效);D1 增量守卫上线;agent DELETE 路由 401(登录守卫正常)
- 云上存量修复 apply(备份 backups/app-state-repair/):19 无身份占位+12 计数漂移+1 完成态含活跃,**44 用户审计异常计数 = none(首次全绿)**
- U3 复测基线:发布时间 2026-07-07 01:15 CST,无身份占位存量 0;之后每一条新增都应被 D1 守卫在 6h 内标失败

## 修复进度追加(2026-07-07 · S4/D6/创建反馈,本地待发布)
- **S4 ✅ 彻底解决(commit f4546f5)**:根因钉死——部署脚本在 npm install(数分钟)前就删 dist,期间旧进程照常服务但静态文件全空,7/1 四次 chunk 报错的 hash 服务器上已不存在,正是踩进该窗口。三件套:①部署零断档(旧 dist 全程服务→build 到 dist-next→旧 assets 带 30 天保留期合并→原子 mv 切换);②版本探测(__BUILD_ID__+version.json,前端 5min+回前台比对,无任务自动软刷新/有任务只提示);③chunk 错刷新延迟 80ms→3s + sessionStorage 防死循环
- **D6 ✅(commit d3670f1)**:死素材(URL 非空但无公网地址、无本地文件可补传)在提交闸口带文件名快速失败,不再放行到策划中途抛模糊错误;JSON 控制字符≈40 经查证系历史存量(全部≈6/5,当前 0 复发,7/7 审计 44 用户 parse 全 OK,活跃写入方均 JSON.stringify),无活体来源,闭环
- **创建反馈慢 ✅(commit c8e16e7)**:根因——即时占位卡只覆盖翻译/万物替换/买家秀;一键主详的卡在素材上传后才建,修图/视频/小红书完全无即时卡。现 immediateProject 扩展到全部模块,一键主详 proj-plan- 卡由策划分支原位接管
- 本批质量:前端 801/801、后端 690/690、脚本 23/23;tsc 干净;lint 0 error;build 成功。**待发布上云**(新部署脚本在下次发布时自动生效,因为它在本地执行)

## 修复进度追加(2026-07-07 · 阶段4/5 完成)
- **阶段5 智能工厂全线打通**:
  - 5-A(commit 800f27e):入口恢复为仅管理员可见(ADMIN_ONLY 导航组+isModuleWithdrawnForUser 单一判据;AI客服保持撤下)
  - 5-C(commit 8652b45):三个真断点修复——①openai_compatible 供应商缺 baseUrl 时回退全局中转 env(此前工厂预览/对话/工具必挂);②preview-turn 双 handler 丢 agentId;③**发布同步桥**(工厂发布→智能体中心物化 draft agent+知识库文档迁移,上线仍走既有验证门禁,重复发布幂等)。侦察报告两处误报被运行态验证纠正('本地chat未接V2'、'缺APIKey配置')
  - 数据:验收残留 agent×4+假中转 provider×2 已清(经 U2 DELETE API,顺带完成 U2 真实验收;备份 internal-store.json.bak-260707-factory-cleanup)
  - E2E 实证:工厂对话真跑通(gpt-5.5+知识库2条引文+工具决策);generate_image 真出图;发布→智能体中心出现同名 agent+3文档已切片
- **阶段4 收尾**(commit a650dca):retry_waiting 任务不再从队列静默消失(单一判据 isActiveTaskStatus 收敛4处硬比对);Toast 去重+上限4条+错误可复制;Suspense 加载 spinner;批量生成卡显示 x/y 进度。侦察15项中4项证伪不动(灯箱按钮浮在深色遮罩上两主题都可读等)
- 回归:前端 801/801、后端 749/749;tsc 干净;lint 0 error;build 成功

## 修复进度追加(2026-07-08 · 业主截图两 bug:知识库删除复活 + Dify 开发标注)
- **知识库"删除了但还在" ✅(commit 6a15d4d,根因库#40)**:不是前端状态残留——`normalizeSmartFactoryConfig` 把"空数组"当"字段缺失"重新播种默认演示库(售后知识库 2 文档,和截图逐字段一致)。纯函数探针复现复活+云库直查实锤(库里只剩复活的默认种子)。修复:仅字段缺失才播种;`modelProviders` 同款问题一并修;3 条回归测试锁持久化回读链路
- **Dify 源码映射开发标注 ✅(commits 1fd2de6/b1c7170)**:'Dify datasets source-mapped' 徽章、'Source: web/app/...' 页脚、源码映射按钮/面板、假"导入 DSL 文件"按钮、'迁移自 Dify...' 文案全部从商户 UI 移除;difyAppStudioSourceMap.ts 留作移植档案(仅测试读取);源码断言测试锁 doesNotMatch /Dify/i
- **已发布腾讯云 ✅**(2026-07-08,过门禁):health OK(worker 2/2 poller);新入口 chunk 无标注(旧 chunk 按保留期正常滞留);零断档部署脚本首次完整跑通含原子切换
- **云端清理 ✅**:按业主删除意图移除复活的售后知识库(备份 /www/backup/system-settings-global-260708-before-kb-cleanup.json),回读 normalize 验证不再复活、供应商配置无损
- 回归:前端 801/801、后端 752/752;lint 0 error;build 成功
- 注意:部署时工作树有另一路在途改动(image-crop 集成,7 文件+新模块,疑似 Codex 执行中),已 stash→部署干净提交→恢复,未随本次发布上云

## 修复进度追加(2026-07-08 · 工厂↔智能体中心打通)
- **批次概括(14 提交,至 733ba90)**:工厂发布=对齐管道(同一 factoryAgentId 重复发布走 upsert 更新而非新建,自动验证+验证通过自动上线);中心侧编辑锁(工厂出品 agent 内容编辑 403 `factory_managed_agent`,status-only PATCH 上下架放行);工厂删除级联下线(中心 agent 转 draft+解锁,不物理删,会话历史保留);关联判据升级为结构化字段 factoryAgentId/factoryKnowledgeBaseId(旧 description marker 仅历史回退);前端工厂徽标+发布/删除反馈
- **本地 E2E 七步验收:22/22 断言全 PASS**(/tmp/factory-e2e.mjs,JSON 模式,重启后进程晚于最后 commit;备份 internal-store.json.bak-260708-factory-e2e):
  1. 工厂建知识库+2文档+agent 绑定 ✅
  2. 首次发布 `agentCenterSync.published=true`,得 agentCenterAgentId=e8babd…ed74 ✅
  3. 中心侧可用:GET /api/agents status=published + factoryAgentId=工厂id;/api/chat/agents 可见;真实对话命中知识库(usedRetrieval=true,回答复述暗号"斑马甲一号")✅
  4. 改 prompt+加第3篇文档再发布:同一 agentCenterAgentId 不新建,版本 1→2,published 版 systemPrompt 含"专家二号",中心物化库文档=3 ✅
  5. 编辑锁:PATCH name→403 `factory_managed_agent`;status-only PATCH draft/published 均 200;POST /draft→403;非工厂 agent 改名不受锁影响 ✅
  6. 级联下线:工厂 DELETE → `agentCenterUnlink.unpublished=true`;中心 agent status=draft、factoryAgentId 清空;chat session 历史仍在;下线后 PATCH name→200(锁解除)✅
  7. durable+冷启动:store 落盘状态正确;launchctl 重启后状态/版本数零漂移 ✅
- E2E 数据已清理(中心 agent/物化知识库/会话/工厂知识库全删,工厂 agent 已随步骤6删除;store 仅剩 3 条历史 log 类无害记录含"E2E"字样)
- **全量回归**:后端 776/776 全过;lint 0 error(659 warning 存量);tsc 干净;build 成功;**前端 866/881,15 失败——均为本批次 733ba90 引入的同一根因**:`src/services/internalApi.ts:32` 引 `factoryManagedConstants` 缺 `.ts` 扩展名(同文件 27/28 行兄弟 import 均带扩展名),`node --experimental-strip-types` 测试跑道解析不了(ERR_MODULE_NOT_FOUND 拖垮 internalApi.test.mjs+smartFactoryPreviewMode.test.mjs 整文件);tsc/vite 均能解析故编译与运行时不受影响。**待修:补 `.ts` 扩展名一处**(本轮验收任务只验不改,留给下一提交)
- **⚠️ 仅本地(JSON 模式)验证,未上云。MySQL 模式代码已对称实现(syncFactoryAgentToDbAgentCenter/unpublishLinkedAgentCenterAgentDb/DB 侧编辑锁)但需上云后按同七步复验**
