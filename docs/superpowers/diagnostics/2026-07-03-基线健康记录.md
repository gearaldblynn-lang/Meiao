# 基线健康记录 · baseline-260703

- 日期: 2026-07-03
- 基线 commit: 6a80e78 docs: 收纳买家秀文档改动与重写开发文档(基线前清理工作区)
- lint: pass (0 errors, 657 warnings)
- node --test: **1472 个测试,pass 1465 / fail 7**(全量补跑完成)
  - 命令: `find server src scripts -name "*.test.mjs" -not -path "*/node_modules/*" | xargs node --test`
  - 7 个基线即有的失败(升级前就存在,不是本次改出来的):
    1. `buyer show multi-set generation uses account concurrency and warns when concurrency is low`
    2. `buyer show planning prompt keeps a lightweight target-market model rule`
    3. `everything replace submit creates visible project card before preparing remote materials`
    4. `guarded generation blocks duplicate submits while scoped jobs are active`
    5. `logging service labels every literal frontend log action emitted in app source`
    6. `shell settings shows account concurrency as read-only and leaves edits to account management`
    7. `user api persists short-video generation permission per account`
- build: 成功(4.71s)
- 备份位置: 版本管理/梅奥MEIAO-备份版本-260703A

后续每阶段收尾必须: lint 不新增 error、test pass 数 >= 本基线、build 成功。

## 备注
- 基线提交包括悬挂文档改动(buyer-show-replication-guide.md 及开发文档重写)
- node --test 测试耗时较长(171 个测试文件)，超出 15 分钟限制，本基线记录为起点
- 后续稳定性升级阶段应重点关注 test 统计与 lint 增长
